{
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "lint": "eslint . --max-warnings=0",
    "test": "node --test",
    "sync:catalogue": "node scripts/sync-catalogue.mjs",
    "check:catalogue": "node scripts/sync-catalogue.mjs --check"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "eslint": "^10.6.0",
    "ignore": "^5.3.2",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
